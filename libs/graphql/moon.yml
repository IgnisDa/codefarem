type: 'library'

tasks:
  # FIXME: Remove once rome supports config files in parent directories
  format:
    args:
      - 'libs/graphql'

  # FIXME: Remove once rome supports config files in parent directories
  lint:
    args:
      - 'libs/graphql'

workspace:
  inheritedTasks:
    exclude:
      - build
      - test
