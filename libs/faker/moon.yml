type: library

tasks:
  # FIXME: Remove once rome supports config files in parent directories
  format:
    args:
      - libs/faker

  # FIXME: Remove once rome supports config files in parent directories
  lint:
    args:
      - libs/faker

workspace:
  inheritedTasks:
    exclude:
      - build
      - test
