type: 'library'

tasks:
  build:
    command: 'swc'
    args:
      - 'src'
      - '--out-dir'
      - 'dist'
      - '--sync'
    outputs:
      - 'dist'

workspace:
  inheritedTasks:
    exclude:
      - 'build'
